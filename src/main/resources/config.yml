# ExoticGarden main configuration
# Config version: 3
# When this version changes, the plugin will back up the old config.yml
# into config-backups before merging in any new default settings.

config-version: 3

grass-drops: []

world-blacklist:
- world_nether
- world_the_end

chances:
  TREE: 22
  BUSH: 16

options:
  auto-update: true
  addon-researches: false
